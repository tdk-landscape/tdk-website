# Canonical marketing numbers — SINGLE SOURCE OF TRUTH for the whole site.
# Change a value here and it updates on every page via {{ site.data.tdk.* }}.
# Every claim below is verified (Sept 2026). Do NOT invent new numbers in
# page HTML — add them here with a comment explaining the source.
#
# NOTE: _config.yml `description` (SEO meta) does NOT process Liquid -
# update it manually to match scale_services / files_per_service.

# --- Product facts (verified against repos on disk) ---
files_per_service: "12-17" # Current .autogenerated/ output range across SDK, backend, and frontend services
sdk_files: 12              # Verified in identity/identity-sdk/.autogenerated/
backend_files: 14          # Verified in identity/identity-management-backend/.autogenerated/
frontend_files: 17         # Verified in identity/identity-management-frontend/.autogenerated/
stack_artifacts: "40+"     # stack-wide: Tiltfile, C4, AGENTS.md, identities, registry, migrators
product_features: "35+"    # feature catalog on /features/
monorepo_services: 27      # service.json files parsed in beauty-crm (do not round this one)
scale_services: "100+"     # 100 microservices in tdk-erp-system, 27 in beauty-crm + platform infra
scale_plain: 100           # same, without "+" — for sentences where "+" reads badly
erp_services: 100          # exact count in tdk-erp-system
erp_domains: 7             # finance, hr, inventory, sales, manufacturing, supply_chain, analytics

# --- Demo facts (the boot-demo stack) ---
demo_services: 14          # services in the demo stack shown in the terminal
demo_artifacts: "168-238"  # demo_services multiplied by the current 12-17 per-service range
demo_boot: "4.6s"          # measured cold boot on `machine` below
demo_discovery: "212ms"    # discovery scan time shown in the terminal
demo_build: "1.8s"         # L4 build time shown in the terminal
boot_claim: "under 5 seconds" # marketing rounding of demo_boot — use this in prose
machine: "16 GB M1 MacBook"   # daily driver the demo was captured on

# --- Calculator parameters (UI bounds + defaults) ---
calc_devs_default: 10
calc_devs_min: 3
calc_devs_max: 120
calc_salary_default: 130000
calc_salary_display: "$130,000"
calc_salary_min: 60000
calc_salary_max: 250000
calc_salary_step: 5000

# --- Onboarding story facts (/features/ "two weeks" narrative) ---
story_devs: 5              # developers hired at once on day one
story_weeks: "two weeks"   # payroll burned on waiting, before TDK
wait_weeks_default: 2      # default "weeks blocked" in the Waiting-for-MVP calculator
setup_weeks: "2–6 weeks"   # K8s-tooling setup cost (stacking card)
onboard_days: "1–3 days"   # per-hire onboarding cost without TDK

# --- Third-party facts (researched, Sept 2026 — re-verify before campaigns) ---
tilt_stars: "9.4k"         # Tilt GitHub stars, "built on" claim
prereq_node: "18+"         # Node.js prerequisite in Quick Start
gitpod_entry: "$20"        # Gitpod/Ona Core entry, per month + compute credits
gitpod_annual_10: "$2,400 + compute" # 10 devs × gitpod_entry × 12
codespaces_hourly: "~$0.36/hr"      # 4-core Codespaces rate
codespaces_monthly: "~$58"          # ≈ full-time month at that rate
codespaces_annual_10: "~$7,000"     # 10 devs full-time, rounded
coder_monthly: "~$50"      # reported Premium per-user level, enterprise sale
coder_annual_10: "~$6,000" # 10 devs at that level, rounded
daytona_vcpu: "~$0.05/vCPU/hr"     # Daytona usage rate
daytona_monthly: "~$140"           # ≈ full-time 4-vCPU month at that rate
daytona_annual_10: "~$16,800"      # 10 devs full-time, rounded
okteto_free: "Free 5 seats"        # Okteto Starter tier
devbox_packages: "80,000+"         # Number of packages available via Nix
devbox_versions: "400,000+"        # Number of package versions available
devenv_languages: "58+"            # Number of supported languages
devenv_services: "42"               # Number of preconfigured services

# --- Lists (rendered with Liquid loops — edit here, updates everywhere) ---
marquee_tools:             # competitors replaced, shown in the homepage marquee
  - "Gitpod"
  - "Codespaces"
  - "Coder"
  - "Daytona"
  - "Okteto"
  - "Garden"
  - "Skaffold"
  - "DevSpace"
  - "Devbox"
  - "Devenv"
  - "Telepresence"
  - "mirrord"
  - "DevPod"
  - "Nocalhost"
  - "vCluster"
  - "Codeanywhere"
  - "Replit"
  - "Hocus"

toolchain_chips:           # real generator outputs, bento strip on homepage
  - "Docker L1–L4 golden layers"
  - "Docker Compose ×8 variants"
  - "Tilt + Starlark engine"
  - "Vite (frontend / backend / SDK / library)"
  - "TypeScript ×5 tsconfig variants"
  - "nginx"
  - "Traefik"
  - "Verdaccio local registry"
  - "Infisical machine identities"
  - "Prisma + DB migrator"
  - "Playwright E2E"
  - "PostgreSQL"
  - "NATS JetStream"
  - "Bun"
  - ".npmrc + .env per service"
  - "C4 architecture diagrams"
  - "AGENTS.md for AI assistants"
  - "Discovery daemon + tdk doctor"

waitlist_features:         # static checkbox options (2 dynamic ones stay in HTML)
  - "No Kubernetes"
  - "Under-5s startup"
  - "Golden Docker L1-L4"
  - "Infisical identities"
  - "C4 diagrams + AGENTS.md"
  - "Terminal UI + doctor"
  - "Selective startup"
  - "Vite + Hot Reload"
  - "Prisma + PostgreSQL"
  - "E2E Playwright tests"

manifest_features:         # service.json "features" — used in /docs/configuration/
  - name: "typescript"
    desc: "TypeScript support"
  - name: "vite"
    desc: "Vite bundler"
  - name: "bun"
    desc: "Bun runtime"
  - name: "node"
    desc: "Node.js runtime"
  - name: "nginx"
    desc: "Nginx reverse proxy"
  - name: "playwright"
    desc: "E2E testing"
  - name: "prisma"
    desc: "Database ORM"

app_types:                 # service.json "appType" — used in /docs/configuration/
  - name: "backend"
    desc: "API service"
  - name: "frontend"
    desc: "Web application"
  - name: "library"
    desc: "Shared code package"
  - name: "worker"
    desc: "Background job processor"
  - name: "migrator"
    desc: "Database migration service"

# --- Licensing facts ---
# The free engine (docker-compose, verdaccio, npm, bun, etc.) needs no key
# at all to run. Issuing a free key (see /#waitlist) is optional and only
# for tracking/future upgrade - see issue-free-license in
# tdk-extension-dist/src/index.ts. The project-ID limit is an anti-sharing
# guard: PROJECT_LIMIT in tdk-extension-dist/src/project-limit.ts.
free_project_limit: 3        # Max distinct project IDs one free license key covers
premium_project_limit: 100   # Max distinct project IDs one premium license key covers

# --- Money facts ---
devops_salary: "$150K"     # avoided platform-engineer hire, USD/year
devops_salary_num: 150000  # same, numeric — used by the ROI calculator JS
devops_monthly: "$12.5K"   # devops_salary ÷ 12 — orbit chip on /compare/
devops_multiple: "65×"     # devops_salary ÷ (10 devs × team_price × 12)
hours_per_week: 5          # recovered engineering hours per dev per week
hours_per_week_num: 5      # same, numeric — used by the ROI calculator JS
weeks_per_year_num: 48     # working weeks in the ROI formula (numeric, JS)
work_hours_num: 2080       # working hours/year in the ROI formula (numeric, JS)
work_hours: "2,080"        # same, display form
cloud_cde_max: "$150"      # top of the Codespaces-class range, per dev/month
cloud_sandbox: "$500"      # hosted-sandbox alternative, per dev/month
team_price: 19             # TDK Team, USD per dev per month
team_annual_10: "$2,280"   # 10 devs × team_price × 12 — keep in sync
community_price: "$0"      # Community tier / "TDK itself costs…" claims
minutes_equiv: "15 minutes" # "$19 is roughly…" — illustrative equivalence
hours_returned: "20 hours"  # "TDK returns about…" — illustrative equivalence
price_multiple_range: "5–500×" # "alternatives cost … more" headline range
roi_example_total: "$300,000" # calculator default (10 devs × $130K): productivity + hire
roi_example_short: "$300K"    # same, short form for headlines
roi_hours_default: "2,400"    # static placeholder before JS runs (10×5×48)
roi_prod_default: "$150,000"  # static placeholder before JS runs

# --- Example-scenario numbers (illustrative, kept consistent) ---
example_devs: 10           # "10 devs" scenario in pricing math + compare matrix
codespaces_range: "$60–150" # Codespaces full-time range, per dev/month
quickstart_time: "under 5 minutes" # Quick Start promise
brownfield_dirs: 31        # directories in the brownfield-rescue example
brownfield_found: 12       # manifests found there
brownfield_synth: 19       # manifests synthesized (dirs − found — keep in sync)
brownfield_artifacts: "372-527" # brownfield_dirs multiplied by the current 12-17 per-service range
brownfield_healthy: "31/31" # resources healthy in that example

# --- Installation commands ---
install_npm: "npm install -g @tdk-landscape/tdk-cli-core"
install_npx_version: "npx @tdk-landscape/tdk-cli-core --version"
install_npx_project: "npx @tdk-landscape/tdk-cli-core project --yes"
install_pnpm: "pnpm add -g @tdk-landscape/tdk-cli-core"
install_bun: "bun add -g @tdk-landscape/tdk-cli-core"
